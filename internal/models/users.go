package models

import (
	"database/sql"
	/* "errors"
	"time" */
)

type UsersModel struct {
	DB *sql.DB
}

/* func (m *UsersModel) Create()(int,error){
	UUID := "sdfsdfs"

	statement := `INSERT INTO users (id,username,email,password,avatar,role)`
} */

